package automotive.common

/*
 * An arbiter forwards input[i] to output[i] for i = [0..N-1] while
 * suspend == false and stops forwarding while suspend == true.
 */
component Arbiter(N=1) {
	in input[N]
	in suspend
	out output[N]
}